import React from "react";

function Header(props) {
      return (
        <h1>
          {/* uncomment the line below to pass the test */}
          {<span>hello from the Header!</span>}
        </h1>
      )
}

export default Header;
